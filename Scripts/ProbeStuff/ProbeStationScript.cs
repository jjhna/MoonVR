using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ProbeStationScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
     /* 
     Steps in order
     1: Press a button for hologram to build engine and axis
         teleports probe to station area, plays lazeers and sounds
     2: use wrench with wheels which will disapear onto attached (teleported) version of probe x6 wheels
        Wheel will be inactive         
     3: use welding tool to meld two poles together also attached (teleported) to probe drop off point
     4: trigger fire event (cannot continue until fire is put out)
        The two poles will be inactive
     5: use screwdriver with pannels to which will all be attached (teleported) to probe drop off point x4 solar panels 
        Pannel will be inactive 
     6: Probe on station will be active for a few seconds, some sounds and goes back to inactive
     */   
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
